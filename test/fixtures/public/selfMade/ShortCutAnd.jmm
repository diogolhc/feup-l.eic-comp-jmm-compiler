import io;

class ShortCutAnd {

    public boolean getBool() {
        return true;
    }

    public int res() {
        boolean a;
        boolean b;
        int res;

        a = false && this.getBool();
        b = true && this.getBool();

        if (a && b) {
            res = 1;
        } else {
            res = 0;
        }

        return res;
    }

    public static void main(String[] args) {
        io.println(new ShortCutAnd().res());
    }
}
