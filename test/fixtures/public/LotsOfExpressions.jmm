import io;
import MathUtils;
import Testing.something.smart;

class Simple {
    public int testing(int a, int b){
            int[] lineLenA;
            int[] lineLenB;
            int[] L;
            int c;
            bool cp;
            c = a * b;
            c = a + MathUtils.random(0-100, 100);
            c = smart;
            b = MathUtils.hello().bye();
            c = a + L[c + 1 * 2];

            cp = c < 2;

            if((c + 2) < a ){
                c  = a-1;
            }else{
                c = b-1;
            }

            return c;
        }
}