import ioPlus;
import BoardBase;
import java.io.File;

class HelloWorld extends BoardBase {
    int i;
    int[] iArray;
/*
    public int mint() {
        return 0;
    }
*/
    public int[] met(int[] j, int k) {
        //j[k] = j[j[j[j[k / this.met(j,k)[-1]] + j[-2 * k] - this.mint()]]];

/*
        while (true && k < 3) {
            if (k < -4) {
                k = 0;
            } else {
                while (true) {
                    k = -1;
                }
            }
        }
*/
        return new H().g();
    }

    public static void main(String[] args) {
        int[] i;
        i = new int[10];
        i[2] = 3;
    }

}
