import ioPlus;
import BoardBase;
import java.io.File;

class HelloWorld extends BoardBase {
    public int met(int i) {
        ioPlus.printHelloWorld();
        3 + this.met(3+4*this.met(4*6));
        return 0;
    }
}
